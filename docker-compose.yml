version: '3.8'

services:
  sikap-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sikap-kemenag-app
    restart: always
    ports:
      - "3000:3000"
      - "8080:8080"
    env_file:
      - .env
